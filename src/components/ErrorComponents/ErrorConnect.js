import React from "react";
import styled from "styled-components";

const StyledError = styled.p`
  margin: 20px;
  font-size: 18px;
`;

const ErrorConnect = () => {
  return <StyledError>Contact the support service...</StyledError>;
};

export default ErrorConnect;
